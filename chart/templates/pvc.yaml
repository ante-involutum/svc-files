apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "data-{{ .Release.Name }}-{{ .Chart.Name }}"
spec:
  storageClassName: local
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: 10Gi

